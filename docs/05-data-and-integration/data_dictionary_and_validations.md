# DOC-DAT-002 — Словарь данных и правила валидации

| Версия | Статус | Дата создания | Дата обновления |
|--------|--------|---------------|-----------------|
| v1.1   | Draft  | 2026-05-07    | 2026-05-30      |

О документе: словарь данных PVZ Monitor и фактические правила валидации при работе с API.

Для кого: backend-разработчики, тестировщик.

Основано на: [логической модели данных](logical_data_model.md).

---

## Авторы документа

- Безручко Александр Вадимович — Backend Developer — валидация данных
- Юркив Альберт Александрович — Backend Developer — логирование ошибок

## История изменений

- [v1.0] [2026-05-07] (Безручко): первичный словарь данных и правила валидации.
- [v1.1] [2026-05-30] (Безручко, Юркив): добавлены словарные статьи для `product_name` и `weight_kg`; добавлены правила валидации V9, V10; обновлены агрегации с учётом новых полей.

---

## Словарь данных

### Таблица `pvz`

| Поле               | Тип     | Nullable | Уникальность | Допустимые значения    | Пример                      |
|--------------------|---------|----------|--------------|------------------------|-----------------------------|
| `pvz_id`           | INTEGER | No       | Yes (PK)     | > 0                    | 1                           |
| `address`          | TEXT    | No       | No           | Непустая строка        | «ул. Ленина, 10, Москва»   |
| `capacity_per_hour`| INTEGER | No       | No           | > 0                    | 50                          |
| `region`           | TEXT    | No       | No           | Непустая строка        | «Центральный»               |

### Таблица `schedule`

| Поле        | Тип     | Nullable | Уникальность           | Допустимые значения       | Пример  |
|-------------|---------|----------|------------------------|---------------------------|---------|
| `id`        | INTEGER | No       | Yes (PK)               | > 0                       | 1       |
| `pvz_id`    | INTEGER | No       | No                     | ∈ pvz.pvz_id              | 1       |
| `weekday`   | INTEGER | No       | Unique (pvz_id+weekday)| 0 (пн) … 6 (вс)          | 0       |
| `open_time` | TEXT    | No       | No                     | HH:MM, 00:00 – 23:59      | «09:00» |
| `close_time`| TEXT    | No       | No                     | HH:MM; > open_time        | «21:00» |

### Таблица `operations`

| Поле          | Тип     | Nullable | Уникальность | Допустимые значения                | Пример              |
|---------------|---------|----------|--------------|------------------------------------|---------------------|
| `op_id`       | INTEGER | No       | Yes (PK)     | > 0                                | 1001                |
| `pvz_id`      | INTEGER | No       | No           | ∈ pvz.pvz_id                       | 2                   |
| `ts`          | TEXT    | No       | No           | ISO 8601, datetime                 | «2026-05-12T10:23» |
| `type`        | TEXT    | No       | No           | `in` \| `out` \| `return`          | «out»               |
| `product_name`| TEXT    | No       | No           | 1–200 символов; DEFAULT 'Не указан'| «Смартфон»          |
| `weight_kg`   | REAL    | No       | No           | ≥ 0, ≤ 1000; DEFAULT 0.0          | 0.35                |

#### Детализация поля `product_name`

| Характеристика     | Значение                                              |
|---------------------|-------------------------------------------------------|
| Тип данных          | TEXT (строка UTF-8)                                   |
| Обязательность      | Да (NOT NULL)                                         |
| Значение по умолчанию | `'Не указан'` — подставляется при пустом значении   |
| Максимальная длина  | 200 символов                                          |
| Формат              | Произвольная строка, название товара                  |
| Примеры             | «Смартфон», «Ноутбук», «Книга», «Одежда», «Мебель»   |
| Источник данных     | Справочник `PRODUCTS_BY_TYPE` в `generate_realistic_data.py` |

#### Детализация поля `weight_kg`

| Характеристика     | Значение                                              |
|---------------------|-------------------------------------------------------|
| Тип данных          | REAL (число с плавающей точкой)                       |
| Обязательность      | Да (NOT NULL)                                         |
| Значение по умолчанию | `0.0`                                               |
| Минимальное значение | 0                                                    |
| Максимальное значение | 1000 (валидация API); CHECK ≥0 на уровне БД         |
| Единица измерения   | Килограммы (кг)                                       |
| Точность            | 2 знака после запятой                                 |
| Примеры             | 0.15 (наушники), 1.50 (ноутбук), 10.0 (продукты)     |
| Распределение       | Равномерное в диапазоне по категории товара           |

### Таблица `error_log`

| Поле       | Тип     | Nullable | Допустимые значения           | Пример                          |
|------------|---------|----------|-------------------------------|---------------------------------|
| `id`       | INTEGER | No       | > 0                           | 55                              |
| `pvz_id`   | INTEGER | No       | любой (может быть невалидным) | 99                              |
| `ts`       | TEXT    | No       | ISO 8601                      | «2026-05-12T07:00»             |
| `op_type`  | TEXT    | No       | любое значение из запроса     | «in»                            |
| `reason`   | TEXT    | No       | строка с описанием            | «pvz_id не найден»              |
| `logged_at`| TEXT    | No       | ISO 8601, datetime            | «2026-05-12T10:30:00»          |

---

## Правила валидации API

### POST /api/operations — создание операции

| №  | Поле          | Правило                                                                   | Ошибка при нарушении                    |
|----|---------------|---------------------------------------------------------------------------|-----------------------------------------|
| V1 | `pvz_id`      | Должен существовать в таблице `pvz`                                       | 400: «ПВЗ {pvz_id} не найден»          |
| V2 | `type`        | Должен быть одним из: `in`, `out`, `return`                               | 400: «Недопустимый тип операции»        |
| V3 | `ts`          | День недели из `ts` должен иметь запись в `schedule` для данного `pvz_id` | 400: «ПВЗ не работает в этот день»     |
| V4 | `ts`          | Время из `ts` должно быть в диапазоне [open_time, close_time]             | 400: «Операция вне рабочих часов»       |
| V5 | Общее         | Запрос должен содержать все три поля: `pvz_id`, `ts`, `type`              | 400: «Отсутствует обязательное поле»    |
| V9 | `product_name`| Строка 1–200 символов; при пустом значении подставляется «Не указан»      | 400: «product_name: максимум 200 символов» |
| V10| `weight_kg`   | Число от 0 до 1000 кг; TypeError/ValueError → 400                         | 400: «weight_kg: от 0 до 1000 кг» / «weight_kg: должно быть числом» |
| V11| `pvz_id` (роль)| Оператор может создавать операции только для своего ПВЗ                  | 403: «Нет доступа к данному ПВЗ»        |

### Валидация в `db.py` — `validate_operations()` (пакетная загрузка)

| №  | Поле          | Правило                                                    | Ошибка при нарушении                          |
|----|---------------|------------------------------------------------------------|-----------------------------------------------|
| V9 | `product_name`| Строка, ≤200 симв.; пустое → подставляется «Не указан»     | reason: «product_name слишком длинный (N симв.)» |
| V10| `weight_kg`   | Число (int/float), ≥ 0                                     | reason: «Некорректный weight_kg: {value}»     |

### Валидации данных расписания (при инициализации)

| № | Правило                                          | Описание                              |
|---|--------------------------------------------------|---------------------------------------|
| V6| `weekday ∈ {0, 1, 2, 3, 4, 5, 6}`               | Недопустимые дни недели отвергаются   |
| V7| `open_time < close_time`                         | Инвертированное расписание недопустимо|
| V8| `capacity_per_hour > 0`                          | Нулевая или отрицательная мощность — ошибка|

---

## Расчёт нагрузки (агрегации)

| Метрика          | Формула                                               | Примечание                               |
|------------------|-------------------------------------------------------|------------------------------------------|
| `ops_per_hour`   | COUNT(op_id) GROUP BY pvz_id, DATE(ts), strftime('%H', ts) | Все типы операций суммируются         |
| `ops_in`         | SUM(CASE WHEN type='in' THEN 1 ELSE 0 END)            | Количество приёмок за интервал          |
| `ops_out`        | SUM(CASE WHEN type='out' THEN 1 ELSE 0 END)           | Количество выдач за интервал            |
| `ops_return`     | SUM(CASE WHEN type='return' THEN 1 ELSE 0 END)        | Количество возвратов за интервал        |
| `total_weight`   | SUM(weight_kg)                                         | Суммарный вес товаров за интервал (кг)  |
| `avg_weight`     | AVG(weight_kg)                                         | Средний вес товара за интервал (кг)     |
| `load`           | ops_per_hour / capacity_per_hour                       | Значение от 0.0 до ~2.0 и выше          |
| `load_pct`       | load × 100                                             | В процентах                              |
| `overload`       | load > 1.0                                             | Булев флаг для подсветки в отчёте       |
| `peak_avg_load`  | AVG(load) WHERE hour ∈ «часы пик»                      | Определяется настройками бизнеса (9–21) |
| `overload_share` | COUNT(overload=True) / COUNT(*) × 100                  | Доля перегруженных интервалов, %        |

---

## Справочник товаров для генерации тестовых данных

Справочник `PRODUCTS_BY_TYPE` в [`generate_realistic_data.py`](../../generate_realistic_data.py:11) определяет категории товаров и диапазоны весов по типам операций:

### Тип «in» (приёмка) — 13 категорий

| Товар          | Вес (кг)     |
|----------------|--------------|
| Смартфон       | 0.15 – 0.50  |
| Ноутбук        | 1.50 – 3.50  |
| Планшет        | 0.40 – 0.90  |
| Наушники       | 0.20 – 0.45  |
| Книга          | 0.20 – 1.50  |
| Одежда         | 0.30 – 2.00  |
| Косметика      | 0.10 – 0.80  |
| Обувь          | 0.50 – 1.50  |
| Игрушка        | 0.15 – 2.00  |
| Бытовая химия  | 0.50 – 5.00  |
| Продукты       | 0.50 – 10.00 |
| Мебель         | 5.00 – 50.00 |
| Аксессуар      | 0.05 – 0.30  |

### Тип «out» (выдача) — 9 категорий

| Товар     | Вес (кг)     |
|-----------|--------------|
| Смартфон  | 0.15 – 0.50  |
| Ноутбук   | 1.50 – 3.50  |
| Планшет   | 0.40 – 0.90  |
| Наушники  | 0.20 – 0.45  |
| Книга     | 0.20 – 1.50  |
| Одежда    | 0.30 – 2.00  |
| Косметика | 0.10 – 0.80  |
| Обувь     | 0.50 – 1.50  |
| Аксессуар | 0.05 – 0.30  |

### Тип «return» (возврат) — 6 категорий

| Товар     | Вес (кг)     |
|-----------|--------------|
| Смартфон  | 0.15 – 0.50  |
| Ноутбук   | 1.50 – 3.50  |
| Одежда    | 0.30 – 2.00  |
| Обувь     | 0.50 – 1.50  |
| Косметика | 0.10 – 0.80  |
| Книга     | 0.20 – 1.50  |
