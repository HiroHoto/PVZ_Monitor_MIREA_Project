# ПВЗ Monitor — Система мониторинга загрузки пунктов выдачи заказов

## Кейс 9: Пункты выдачи маркетплейса (e-commerce / логистика)

### Быстрый старт

```bash
# 1. Установить зависимости
pip install flask

# 2. Запустить приложение
cd pvz_system
python app.py

# 3. Открыть браузер
# http://127.0.0.1:5000
```

---

## Демо-аккаунты

| Логин | Пароль | Роль | Доступ |
|-------|--------|------|--------|
| `operator1` | `op1pass` | Оператор ПВЗ | Только ПВЗ №1 |
| `operator2` | `op2pass` | Оператор ПВЗ | Только ПВЗ №2 |
| `supervisor1` | `suppass` | Супервайзер | Регион Центральный |
| `analyst1` | `anapass` | Аналитик | Все ПВЗ + экспорт |

---

## Схема базы данных

```sql
pvz (pvz_id, address, capacity_per_hour, region)
schedule (id, pvz_id, weekday 0-6, open_time, close_time)
operations (op_id, pvz_id, ts DATETIME, type IN {in,out,return})
error_log (id, pvz_id, ts, op_type, reason, logged_at)
```

---

## Валидации (5 обязательных)

1. **Ссылочная целостность** — pvz_id должен существовать в таблице pvz
2. **Корректность расписания** — open_time < close_time; weekday ∈ {0..6}
3. **Типы операций** — type ∈ {in, out, return}
4. **Рабочие часы** — operations.ts должен попадать в открытые часы ПВЗ
5. **День без расписания** — операции в дни без расписания запрещены

Все ошибки валидации логируются в таблицу `error_log`.

---

## API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/login` | Авторизация |
| POST | `/api/logout` | Выход |
| GET | `/api/me` | Текущий пользователь |
| GET | `/api/pvz` | Список ПВЗ (с учётом роли) |
| GET | `/api/pvz/<id>` | Детали ПВЗ + расписание |
| GET | `/api/regions` | Список регионов |
| GET | `/api/operations` | Операции с фильтрами |
| POST | `/api/operations` | Добавить операцию |
| GET | `/api/report/load` | Отчёт загрузки (pvz_id, date, hour, ops, capacity, load) |
| GET | `/api/report/heatmap` | Тепловая карта (день × час) |
| GET | `/api/export/csv` | Экспорт отчёта в CSV |
| GET | `/api/errors` | Журнал ошибок данных |

### Фильтры для `/api/report/load`:
- `pvz_id` — конкретный ПВЗ
- `region` — регион
- `type` — тип операции (in/out/return)
- `date_from` / `date_to` — диапазон дат (YYYY-MM-DD)

---

## KPI

- **Средняя загрузка** = среднее(ops_per_hour / capacity_per_hour) за период
- **Загрузка в часы пик** = средняя загрузка в 12–14 и 18–20 ч.
- **Доля перегрузок** = кол-во интервалов с load > 100% / всего интервалов

---

## Демо-сценарий

1. Войти как `analyst1` / `anapass`
2. На дашборде посмотреть KPI и графики за Март 2025
3. Перейти в «Отчёт загрузки», отфильтровать по ПВЗ №1
4. В «Тепловой карте» выбрать ПВЗ и построить карту по часам/дням
5. Экспортировать CSV через кнопку «CSV»
6. Добавить операцию через меню «Добавить операцию» — проверить валидацию (попробовать указать нерабочий час)
7. Просмотреть «Журнал ошибок»
8. Выйти и войти как `operator1` — убедиться, что доступен только ПВЗ №1

---

## Структура проекта

```
pvz_system/
├── app.py              — Flask-приложение (backend + API)
├── requirements.txt    — Зависимости
├── README.md           — Документация
├── pvz.db              — SQLite база данных (создаётся автоматически)
├── data/
│   ├── pvz.json        — Справочник ПВЗ (6 точек)
│   ├── schedule.json   — Расписание (39 записей)
│   └── operations.json — Операции (25 000+, за март 2025)
└── templates/
    └── index.html      — SPA фронтенд
```

---

## Технологии

- **Backend**: Python 3.10+, Flask 3.x, SQLite (встроена в Python)
- **Frontend**: Vanilla HTML/CSS/JS, Chart.js 4.4, Lucide Icons
- **Данные**: синтетическая выборка ≥25 000 операций за март 2025
